FROM node:22-alpine

WORKDIR /app

# Install dependencies
COPY apps/api/package.json ./
RUN npm install

# Copy source (will be overridden by volume mount in dev)
COPY apps/api/ .

EXPOSE 3001

CMD ["npm", "run", "dev"]
